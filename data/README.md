# Data

Generated CSV summaries and larger numerical outputs can live here. The default
`.gitignore` excludes generated CSV and JSON files so classroom experiments do
not accidentally become repository noise.

